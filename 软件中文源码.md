# 总体架构
## 服务端
常驻在系统中，用于执行长任务以及为app提供系统api使用

## APP
管理服务，以及利用服务完成简单事情

分为App、界面（ViewModel+ComposeUI）。

ComposeUI->ViewModel->App->服务，信息逐级传递不可跨越。

# 服务端
一种自定义服务，可使用Shizuku/Root启动。启动后常驻系统，App随时可连接。

# 任务功能
因为网卡只能同时执行一个任务，所以被设计为同时只能同时运行一个，运行于服务端。
任务拥有自身状态（运行中或已结束）与任务ID（用于绑定）

## 界面显示、服务通信
由App向服务发送任务启动、停止、更新命令。每种任务都有各自的任务启动传参、更新传参。
ComposeUI可向viewmodel请求启动任务，viewmodel记录自身显示此任务id，UI订阅viewmodel的焦点任务id，Sheet显示。结束时还是显示此任务id的任务，只不过是结束态，直到手动关闭Sheet。

## 类型
### 连接网络-ConnectWifiTask

### 使用PIN按钮保存网络-WpsPbcTask
